[project]
name = "ai-image-tools"
version = "0.1.0"
description = "MCP server for AI image generation using Google's Imagen API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.2.0",
    "google-generativeai>=0.8.0",
]
